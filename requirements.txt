# THE TWILIGHT OF ANTIBIOTICS — AMR Research Dashboard
# Pinned versions matching the local venv (Python 3.12).
# These are the runtime dependencies for the Dash app under dashboard/.

dash==4.1.0
plotly==6.6.0
pandas==3.0.2
numpy==2.4.4
statsmodels==0.14.6
Flask==3.1.3

# Production WSGI server — recommended for deployment behind a reverse proxy
# (DigitalOcean App Platform, Render, Fly.io). Local dev uses Flask's built-in
# server via `python dashboard/app.py`.
gunicorn>=21.2.0
